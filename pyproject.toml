[project]
name = "amelie-core"
version = "0.1.0"
description = "Core engine for Amelie Suite"
authors = [{ name = "Edwin José Nolasco" }]
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.pytest.ini_options]
pythonpath = ["src"]

[tool.ruff]
line-length = 100